apiVersion: apps/v1
kind: Deployment
metadata:
  name: star-agile-healthcare-project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: star-agile-healthcare-project
  template:
    metadata:
      labels:
        app: star-agile-healthcare-project
    spec:
      containers:
      - name: star-agile-healthcare-project
        image: snehakamble/star-agile-healthcare-project:latest
        ports:
        - containerPort: 8081
