apiVersion: apps/v1
kind: Deployment
metadata:
  name: star-agile-insurance-project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: star-agile-insurance-project
  template:
    metadata:
      labels:
        app: star-agile-insurance-project
    spec:
      containers:
      - name: star-agile-insurance-project
        image: snehakamble/star-agile-insurance-project:latest
        ports:
        - containerPort: 8081
